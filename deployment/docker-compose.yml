version: '3.8'

services:
  loophive:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ../storage:/app/storage
    env_file:
      - ../.env
    restart: unless-stopped
